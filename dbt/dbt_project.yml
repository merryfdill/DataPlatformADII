name: 'adii_platform'
version: '1.0.0'
config-version: 2

profile: 'adii_platform'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  adii_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
